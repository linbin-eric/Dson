# 需求.md

## 需求概述

1. 本次需求目标是移除当前项目内的 `cc.jfire.dson.reader.support.TypeResolver`，改为使用 `cc.jfire.baseutil.reflect.type.TypeResolver` 承担泛型类型解析职责。
2. 替换范围包含标准反序列化路径和编译反序列化路径，确保项目内不再引用自定义 `reader.support.TypeResolver`。
3. 本次需求接受对 `TypeReader` 公开方法签名进行调整，并同步更新使用文档。

## 功能详情

4. 删除 `src/main/java/cc/jfire/dson/reader/support/TypeResolver.java` 源文件。
5. 将 `Dson` 中的类型解析缓存从 `Map<TypeVariable<?>, Type>` 上下文模型调整为基于 `cc.jfire.baseutil.reflect.type.TypeResolver` 的实例模型。
6. 将 `DsonContext` 中反序列化 reader 的解析流程改为通过 baseutil `TypeResolver` 解析待处理 `Type`。
7. 将 `TypeReader.initialize` 和 `TypeReader.fromString` 的签名从传递 `Map<TypeVariable<?>, Type>` 改为传递 baseutil `TypeResolver` 或等价的新上下文对象。
8. 将 `Node.generateRoot`、`StandardReadEntry`、`ReadEntry.compile`、`CollectionReader`、`NewArrayReader`、`ObjectReader` 以及其他直接依赖项目内 `TypeResolver` 的类同步改为使用 baseutil `TypeResolver`。
9. 替换动态编译生成代码中硬编码的 `cc.jfire.dson.reader.support.TypeResolver.resolveTypeArguments(type)` 调用。
10. 保持现有对普通泛型继承、集合、Map、泛型数组、通配符上界等场景的反序列化能力。
11. 不要求本次额外增强泛型内部类 `ParameterizedType.ownerType` 场景，只要求当前测试用例能通过。

## 业务规则

12. 项目内不得再出现 `cc.jfire.dson.reader.support.TypeResolver` 的 import、全限定名引用或源码定义。
13. baseutil `TypeResolver` 是唯一允许用于反序列化泛型解析的 TypeResolver 实现。
14. 替换后 reader 缓存不能因为泛型参数不同而相互污染，例如 `Response<String>` 与 `Response<Row>` 必须解析为各自正确的 reader 行为。
15. 标准反序列化和编译反序列化应保持语义一致。
16. 自定义 `TypeReader` 使用方需要按新签名迁移，文档必须反映这一破坏性变更。

## 输入输出

17. 输入为现有 Java 源码、测试代码、README 和 API 指南文档。
18. 输出为完成替换后的 Java 源码、已删除的项目内 `TypeResolver`、更新后的文档，以及通过验证的测试结果。
19. 对外 JSON 反序列化 API 的核心使用方式应尽量保持不变，例如 `Dson.fromString(type, json)` 和 `Dson.fromStringByCompile(type, json)` 继续可用。

## 异常处理

20. 当 baseutil `TypeResolver` 无法解析某个类型变量时，应保持当前项目的合理降级行为，不应引入非预期的空指针异常。
21. 对当前测试未覆盖的泛型内部类 ownerType 差异，不作为本次需求的失败条件。
22. 如果迁移过程中发现 baseutil `TypeResolver` 无法覆盖现有测试场景，应优先调整调用方式；只有在必须修改 baseutil 依赖时，才进入额外方案确认。

## 技术要求

23. 项目继续使用当前 Maven 和 Java 17 构建方式。
24. 不引入新的第三方依赖。
25. 更新 `README.md` 和 `dson-api-guide.html` 中涉及 `TypeReader` 自定义反序列化示例及签名说明的内容。
26. 保持代码风格与当前项目一致，避免无关重构。

## 验收标准

27. `rg "cc.jfire.dson.reader.support.TypeResolver|reader.support.TypeResolver|class TypeResolver" src/main/java README.md dson-api-guide.html` 不应再命中项目内旧 TypeResolver 的有效引用。
28. `mvn -q test` 必须通过。
29. 现有泛型反序列化相关测试必须通过，包括标准模式、编译模式、泛型数组、缓存污染、通配符上界等测试。
30. README 和 API 指南中的自定义 `TypeReader` 示例与最终代码签名一致。
31. 删除项目内 `TypeResolver` 后，项目必须能完成完整编译。
