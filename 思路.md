在构建TypeReader 的时候，
我是需要知道两个信息：
1、当前的类定义
2、当前的实际 Type，这个 Type 就可以携带泛型信息

那下一个问题

如果是 A extend B<User>,  B<T> extends C<T>

那么到了 B 这边，获取父类泛型信息的时候只剩下占位符 T 了。
这样推测，当我们有类定义，类实际 Type，我们还需要构建


所以从归因的角度，我们需要一个上下文，是一个 map，key 是 class A，value 是 Map，
map 的key 是字符串，也就是在 class A 中定义的泛型名称，值是 class，也就是这个泛型名称在本次实际的类型