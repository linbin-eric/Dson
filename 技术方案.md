# 技术方案.md

### 步骤 1: 顶层反序列化类型上下文改造
- **对应需求**: 需求 1. 2. 5. 13. 19.
- **方案深度**: 标准
- **现状与约束**: `Dson` 当前通过 `ConcurrentMap<Type, ConcurrentMap<TypeVariable<?>, Type>> typeResolvers` 缓存项目内 `TypeResolver.resolveTypeArguments` 的结果，并把 `Map<TypeVariable<?>, Type>` 传给 `DsonContext.fromString`。baseutil `TypeResolver` 是实例式 API，只提供 `new TypeResolver(Type source)` 和 `resolveType(Type type)`。
- **实现方式**:
  - 将 `Dson.typeResolvers` 改为 `ConcurrentMap<Type, cc.jfire.baseutil.reflect.type.TypeResolver>`。
  - `fromString(Type, String)`、`fromStringByCompile(Type, String)`、`fromString(String)`、`fromStringByAttribute(...)` 中使用 `computeIfAbsent(type, TypeResolver::new)` 创建并缓存根类型 resolver。
  - 保持现有对外 API 不变，调用者仍使用 `Dson.fromString(type, json)` 和 `Dson.fromStringByCompile(type, json)`。
  - 不在 `Dson` 中保留任何项目内 `cc.jfire.dson.reader.support.TypeResolver` 的 import 或全限定名。
- **影响范围**: `src/main/java/cc/jfire/dson/Dson.java`。
- **验收与检查点**: `Dson` 中不再出现 `Map<TypeVariable<?>, Type>` 类型解析缓存；公开 JSON 反序列化入口签名不变。
- **参考文档**: `需求.md`；`src/main/java/cc/jfire/dson/Dson.java`；`~/.m2/repository/cc/jfire/baseutil/1.0.1-SNAPSHOT/baseutil-1.0.1-SNAPSHOT-sources.jar!/cc/jfire/baseutil/reflect/type/TypeResolver.java`。

### 步骤 2: DsonContext reader 解析与缓存模型改造
- **对应需求**: 需求 2. 6. 10. 14. 15. 20. 22.
- **方案深度**: 详细
- **现状与约束**: `DsonContext.parseReader(Type, Map<TypeVariable<?>, Type>)` 当前先用项目内 resolver 解析 `lookupType`，再以 `lookupType` 作为缓存键创建 reader。这个缓存键策略需要保留，否则 `Response<String>`、`Response<Row>`、泛型数组等场景可能发生 reader 污染。
- **实现方式**:
  - 将 `parseReader` 签名调整为 `parseReader(Type type, cc.jfire.baseutil.reflect.type.TypeResolver typeResolver)`。
  - 使用 `typeResolver.resolveType(type)` 计算 `lookupType`；若 `typeResolver` 为 `null`，使用 `new TypeResolver(type)` 作为根 resolver。
  - 保留 `lookupType` 作为 `readers` 和 `CURRENT_READER_CACHE` 的 key。
  - 创建新 reader 后，为当前 `lookupType` 创建作用域 resolver：`TypeResolver readerResolver = new TypeResolver(lookupType)`，再传给 `typeReader.initialize(lookupType, this, readerResolver)`。
  - 移除 `needResolve` 和 `containsResolvableTypeVariable` 两个基于 `Map<TypeVariable<?>, Type>` 的辅助方法，解析职责交给 baseutil `TypeResolver`。
  - `fromString` 和 `fromStringByAttribute` 改为接收并传递 baseutil resolver。
  - 对 `resolveType` 后仍为无法处理的 `TypeVariable` 的场景保持现有失败语义，不额外吞异常；当前测试未覆盖 ownerType 增强，不作为本次目标。
- **影响范围**: `src/main/java/cc/jfire/dson/DsonContext.java`；所有调用 `parseReader`、`fromString`、`fromStringByAttribute` 的反序列化路径。
- **验收与检查点**: reader 缓存按解析后的 `lookupType` 区分不同泛型实参；`ReaderCachePollutionTest`、`GenericArrayReaderCachePollutionTest`、泛型反序列化测试通过。
- **参考文档**: `需求.md`；`src/main/java/cc/jfire/dson/DsonContext.java`；`src/test/java/cc/jfire/dson/paramtiez/ReaderCachePollutionTest.java`；`src/test/java/cc/jfire/dson/paramtiez/GenericArrayReaderCachePollutionTest.java`。

### 步骤 3: TypeReader 接口与动态编译代码改造
- **对应需求**: 需求 3. 7. 9. 15. 16.
- **方案深度**: 详细
- **现状与约束**: `TypeReader` 当前公开方法签名包含 `Map<TypeVariable<?>, Type>`，并在动态生成代码中硬编码 `cc.jfire.dson.reader.support.TypeResolver.resolveTypeArguments(type)`。这是旧 resolver 最关键的耦合点。
- **实现方式**:
  - 将接口签名调整为：
    ```java
    default void initialize(Type type, DsonContext dsonContext, TypeResolver typeResolver) {}
    Object fromString(Stream stream, TypeResolver typeResolver);
    ```
    其中 `TypeResolver` 使用 `cc.jfire.baseutil.reflect.type.TypeResolver`。
  - 批量更新所有 `TypeReader` 实现类的方法签名，包括基础类型 reader、数组 reader、`ObjectReader`、`CollectionReader`、`MapReader`、`NewArrayReader`、`UnKnowTypeReader`、`ReadObjectAsString` 等。
  - 将 `TypeReader.compile` 生成代码中的 `readerTypeVariableContext` 字段替换为 `readerTypeResolver` 字段。
  - 动态编译初始化体中使用 `new cc.jfire.baseutil.reflect.type.TypeResolver(type)` 构建当前 reader 的作用域 resolver，不再生成 `cc.jfire.dson.reader.support.TypeResolver.resolveTypeArguments(type)`。
  - 生成代码中所有 `dsonContext.parseReader(..., thisTypeVariableContext)`、`typeReader.initialize(..., thisTypeVariableContext)`、`typeReader.fromString(..., readerTypeVariableContext)` 改为传递当前作用域 `TypeResolver`。
  - 保留现有内联数组、集合、Map 的性能优化分支，但它们获取子 reader 时必须通过 baseutil resolver 解析字段泛型。
- **影响范围**: `src/main/java/cc/jfire/dson/reader/TypeReader.java`；`src/main/java/cc/jfire/dson/reader/impl/**`；`src/main/java/cc/jfire/dson/reader/impl/basic/**`。
- **验收与检查点**: 标准模式和 `Dson.fromStringByCompile` 均可编译运行；动态编译生成代码不再含旧 resolver 全限定名；自定义 `TypeReader` 编译签名与文档一致。
- **参考文档**: `需求.md`；`src/main/java/cc/jfire/dson/reader/TypeReader.java`；`src/test/java/cc/jfire/dson/paramtiez/GenericResponseByCompileDeserializeTest.java`。

### 步骤 4: 对象字段、ReadEntry 与容器 reader 的作用域 resolver 传递
- **对应需求**: 需求 6. 8. 10. 14. 15. 20.
- **方案深度**: 详细
- **现状与约束**: `Node.generateRoot`、`StandardReadEntry`、`ReadEntry.compile`、`CollectionReader`、`NewArrayReader` 直接依赖项目内 resolver 的静态方法。字段解析必须在“当前 reader 对应的 resolved type”作用域下进行，否则 `Pair<L, R>`、`PageData<T>`、`T[]` 等嵌套泛型会丢失具体实参。
- **实现方式**:
  - `Node.generateRoot(Class, Type, DsonContext, TypeResolver)` 直接接收当前 reader 的作用域 resolver，不再自行合并 `Map`。
  - `StandardReadEntry` 保存 `TypeResolver typeResolver`，通过 `typeResolver.resolveType(field.getGenericType())` 判断字段实际类型和 classId。
  - `StandardReadEntry` 为复杂字段创建子 reader 时调用 `dsonContext.parseReader(field.getGenericType(), typeResolver)`，由 `DsonContext` 解析成具体 `lookupType` 并初始化子 reader 的独立作用域 resolver。
  - `ReadEntry.compile` 与 `StandardReadEntry` 保持相同传参模型，生成类字段从 `Map typeVariableContext` 改为 `TypeResolver typeResolver`。
  - `CollectionReader` 在 `initialize` 阶段先解析自身 type，再解析集合元素 type；`fromString` 调用元素 reader 时传递 reader 自身保存的 resolver。
  - `NewArrayReader` 在处理 `GenericArrayType` 时用 resolver 解析最终组件类型，确保 `T[]` 在不同子类中生成不同组件类型数组。
  - `MapReader` 接收的 type 应来自 `DsonContext` 的 `lookupType`；必要时在初始化开头再次执行 `typeResolver.resolveType(type)` 作为防御。
- **影响范围**: `src/main/java/cc/jfire/dson/reader/support/Node.java`；`src/main/java/cc/jfire/dson/reader/support/entry/ReadEntry.java`；`src/main/java/cc/jfire/dson/reader/support/entry/StandardReadEntry.java`；`src/main/java/cc/jfire/dson/reader/impl/ObjectReader.java`；`src/main/java/cc/jfire/dson/reader/impl/CollectionReader.java`；`src/main/java/cc/jfire/dson/reader/impl/MapReader.java`；`src/main/java/cc/jfire/dson/reader/impl/NewArrayReader.java`。
- **验收与检查点**: `GenericResponseDeserializeTest` 中普通继承泛型、嵌套参数化类型、Map、泛型数组、通配符上界场景通过；不同泛型实参的 reader 不复用错误状态。
- **参考文档**: `需求.md`；`src/test/java/cc/jfire/dson/paramtiez/GenericResponseDeserializeTest.java`；`src/test/java/cc/jfire/dson/paramtiez/WildcardUpperBoundDeserializeTest.java`。

### 步骤 5: 删除旧 TypeResolver 并清理引用
- **对应需求**: 需求 1. 4. 12. 13. 17. 18. 27. 31.
- **方案深度**: 简略
- **现状与约束**: 当前存在 `src/main/java/cc/jfire/dson/reader/support/TypeResolver.java`，并被多个类 import 或通过全限定名引用。
- **实现方式**:
  - 删除 `src/main/java/cc/jfire/dson/reader/support/TypeResolver.java`。
  - 清理所有 `cc.jfire.dson.reader.support.TypeResolver` import。
  - 全项目搜索旧全限定名，尤其是 `TypeReader.compile` 生成字符串中的硬编码引用。
  - 保留 `cc.jfire.baseutil.reflect.type.TypeResolver` 作为唯一反序列化泛型解析实现。
- **影响范围**: `src/main/java/cc/jfire/dson/reader/support/TypeResolver.java`；所有旧 resolver 引用点。
- **验收与检查点**: `rg "cc.jfire.dson.reader.support.TypeResolver|reader.support.TypeResolver|class TypeResolver" src/main/java README.md dson-api-guide.html` 不再命中旧实现有效引用。
- **参考文档**: `需求.md`。

### 步骤 6: 文档与自定义 TypeReader 示例更新
- **对应需求**: 需求 16. 17. 18. 25. 30.
- **方案深度**: 标准
- **现状与约束**: `README.md` 和 `dson-api-guide.html` 中的自定义 `TypeReader` 示例仍使用旧的 `fromString(Stream stream)` 签名，与当前实现已有差异，也会与迁移后的新签名不一致。
- **实现方式**:
  - 更新 `README.md` 中 `@DeSerializeDefinition` 和 `TypeReader（反序列化）` 示例。
  - 更新 `dson-api-guide.html` 中自定义反序列化和字段转换器示例。
  - 示例统一使用：
    ```java
    public Object fromString(Stream stream, TypeResolver typeResolver)
    ```
    并补充 `cc.jfire.baseutil.reflect.type.TypeResolver` import。
  - 文档说明这是自定义反序列化器需要实现的新签名；一般简单 reader 可以忽略 `typeResolver` 参数。
- **影响范围**: `README.md`；`dson-api-guide.html`。
- **验收与检查点**: 文档示例与最终 `TypeReader` 接口签名一致；文档中不再出现旧 resolver 的有效引用。
- **参考文档**: `需求.md`；`README.md`；`dson-api-guide.html`。

### 步骤 7: 编译、测试与回归检查
- **对应需求**: 需求 10. 11. 14. 15. 20. 21. 22. 23. 24. 26. 27. 28. 29. 31.
- **方案深度**: 标准
- **现状与约束**: 当前项目使用 Maven 与 Java 17，已有泛型反序列化测试覆盖标准模式、编译模式、泛型数组、缓存污染、通配符上界。需求明确不新增第三方依赖，不要求 ownerType 额外增强。
- **实现方式**:
  - 运行 `mvn -q -DskipTests compile` 验证源码和动态签名更新后的编译结果。
  - 运行 `mvn -q test` 验证完整测试集。
  - 重点关注 `src/test/java/cc/jfire/dson/paramtiez/*` 下测试。
  - 运行旧引用清理命令：
    ```bash
    rg "cc.jfire.dson.reader.support.TypeResolver|reader.support.TypeResolver|class TypeResolver" src/main/java README.md dson-api-guide.html
    ```
  - 若 baseutil `TypeResolver` 在当前测试覆盖场景中解析不足，优先调整 Dson 内部调用作用域和缓存键；只有无法通过调用方式解决时，才返回需求阶段确认是否修改 baseutil。
- **影响范围**: 构建与测试流程；不涉及生产逻辑新增依赖。
- **验收与检查点**: 完整测试通过；旧 resolver 引用清零；无新增依赖；Java 17 Maven 构建通过。
- **参考文档**: `pom.xml`；`src/test/java/cc/jfire/dson/paramtiez/GenericResponseDeserializeTest.java`；`src/test/java/cc/jfire/dson/paramtiez/GenericResponseByCompileDeserializeTest.java`；`src/test/java/cc/jfire/dson/paramtiez/ReaderCachePollutionTest.java`；`src/test/java/cc/jfire/dson/paramtiez/GenericArrayReaderCachePollutionTest.java`；`src/test/java/cc/jfire/dson/paramtiez/WildcardUpperBoundDeserializeTest.java`。

## 需求覆盖矩阵

| 需求编号 | 覆盖步骤 |
|---|---|
| 1 | 步骤 1, 步骤 5 |
| 2 | 步骤 1, 步骤 2 |
| 3 | 步骤 3, 步骤 6 |
| 4 | 步骤 5 |
| 5 | 步骤 1 |
| 6 | 步骤 2, 步骤 4 |
| 7 | 步骤 3 |
| 8 | 步骤 4 |
| 9 | 步骤 3, 步骤 5 |
| 10 | 步骤 2, 步骤 4, 步骤 7 |
| 11 | 步骤 2, 步骤 7 |
| 12 | 步骤 5 |
| 13 | 步骤 1, 步骤 5 |
| 14 | 步骤 2, 步骤 4, 步骤 7 |
| 15 | 步骤 2, 步骤 3, 步骤 4, 步骤 7 |
| 16 | 步骤 3, 步骤 6 |
| 17 | 步骤 5, 步骤 6 |
| 18 | 步骤 5, 步骤 6 |
| 19 | 步骤 1 |
| 20 | 步骤 2, 步骤 4, 步骤 7 |
| 21 | 步骤 7 |
| 22 | 步骤 2, 步骤 7 |
| 23 | 步骤 7 |
| 24 | 步骤 7 |
| 25 | 步骤 6 |
| 26 | 步骤 7 |
| 27 | 步骤 5, 步骤 7 |
| 28 | 步骤 7 |
| 29 | 步骤 7 |
| 30 | 步骤 6 |
| 31 | 步骤 5, 步骤 7 |
