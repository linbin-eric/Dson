# Compile 模式性能优化实施计划

## 当前前提

- 已确认 `Stream` 构造函数中的 `str.length() < value.length` 已改为 `<=`，重复 `char[]` 分配问题已修复。
- 后续优化只针对 compile 模式，不改变默认 `ObjectReader` 的设计路径。
- 每一步单独提交级别地实施、测试、性能对比，避免多个变化混在一起无法判断收益。

## 验证命令

每一步完成后先跑功能测试：

```bash
mvn test
```

然后重新生成 JMH classpath：

```bash
mvn -q dependency:build-classpath -Dmdep.outputFile=target/test-classpath.txt -DincludeScope=test
```

再跑吞吐与分配：

```bash
java -cp "target/test-classes:target/classes:$(cat target/test-classpath.txt)" \
  org.openjdk.jmh.Main 'BenchmarkBig.test(Compile|New)$' \
  -wi 3 -i 5 -w 1s -r 1s -f 1 -tu ms -prof gc
```

最后跑 compile 模式 JFR 热点：

```bash
rm -f target/jfr-compile-step.jfr
java -cp "target/test-classes:target/classes:$(cat target/test-classpath.txt)" \
  org.openjdk.jmh.Main 'BenchmarkBig.testCompile$' \
  -wi 5 -i 8 -w 2s -r 2s -f 1 -tu ms \
  -jvmArgsAppend '-XX:StartFlightRecording=filename=target/jfr-compile-step.jfr,settings=profile,dumponexit=true'
```

查看 JFR：

```bash
$(/usr/libexec/java_home)/bin/jfr view --width 180 --cell-height 8 hot-methods target/jfr-compile-step.jfr
$(/usr/libexec/java_home)/bin/jfr view --width 180 --cell-height 8 allocation-by-site target/jfr-compile-step.jfr
```

## 实施顺序

### 第 1 步：compile 专属字段 id 分派

目标：

- 当前 compile reader 先通过 `Stream.getName(rootNode)` 匹配到 `ReadEntry`，再通过 `switch(readEntry.name())` 做字符串分派。
- 改为 compile reader 专属字段名 trie，字段名解析后直接返回 `int fieldIndex`。
- 生成代码使用 `switch(fieldIndex)`，避免第二次字符串分派。

修改范围：

- 新增 `cc.jfire.dson.reader.support.FieldIndexNode`
- `Stream` 新增 `getNameIndex(FieldIndexNode node)`
- `TypeReader.compile` 生成 `FieldIndexNode rootNode` 和 `switch(int)`

判断标准：

- `mvn test` 通过。
- `BenchmarkBig.testCompile` 吞吐不能明显退化。
- JFR 中 `String.charAt`、`BeanReader_0.fromString`、`Stream.getName` 相关占比观察是否下降。

结果：

- `mvn test` 通过：`Tests run: 133, Failures: 0, Errors: 0, Skipped: 1`
- JMH GC：`BenchmarkBig.testCompile = 435.143 ± 4.548 ops/ms`
- JMH GC：`BenchmarkBig.testCompile:·gc.alloc.rate.norm = 5537.074 ± 0.207 B/op`
- JFR：`BenchmarkBig.testCompile = 441.043 ± 2.533 ops/ms`
- JFR hot methods 前列：
  - `Stream.getString()`：`16.79%`
  - `String.charAt(int)`：`13.53%`
  - `Stream.getNameIndex(FieldIndexNode)`：`9.26%`
  - `ClassIntArrayReader.fromString(...)`：`8.30%`
  - `BeanReader_0.fromString(...)`：`7.25%`

判断：

- 字段 id 分派没有增加分配，`B/op` 基本维持在 `5537`。
- `BeanReader_0.fromString` 占比从上一步约 `9.23%` 降到 `7.25%`。
- 但字段名解析成本仍在，热点从 `Stream.getName(Node)` 转移为 `Stream.getNameIndex(FieldIndexNode)`。
- `String.charAt` 没有明显下降，说明它主要不是 `switch(readEntry.name())` 的成本，或者 JFR 归因混入了其他字符串路径。

状态：已完成。

### 第 2 步：字段专属一维数组 reader

目标：

- 对 compile reader 中明确字段类型的一维数组生成内联读取代码。
- 首批覆盖 `int[]`、`char[]`、`String[]`、`Integer[]`、普通 POJO 一维数组。

判断标准：

- `ClassIntArrayReader.fromString`、`IntArrayReader.fromString`、`CharArrayReader.fromString`、`NewArrayReader.fromString` 在 JFR 中下降。
- `BenchmarkBig.testCompile` 的 `B/op` 不明显回升。

结果：

- `mvn test` 通过：`Tests run: 133, Failures: 0, Errors: 0, Skipped: 1`
- JMH GC：`BenchmarkBig.testCompile = 392.674 ± 123.537 ops/ms`
- JMH GC：`BenchmarkBig.testCompile:·gc.alloc.rate.norm = 5536.744 ± 0.087 B/op`
- JMH GC：`BenchmarkBig.testNew = 507.026 ± 30.877 ops/ms`
- JMH GC：`BenchmarkBig.testNew:·gc.alloc.rate.norm = 5664.627 ± 0.073 B/op`
- JFR：`BenchmarkBig.testCompile = 442.761 ± 6.586 ops/ms`
- JFR hot methods 前列：
  - `Stream.getString()`：`14.65%`
  - `String.charAt(int)`：`12.44%`
  - `BeanReader_0.fromString(...)`：`11.79%`
  - `Stream.getNameIndex(FieldIndexNode)`：`9.93%`
  - `BeanReader_11.fromString(...)`：`8.63%`
  - `CollectionReader.fromString(...)`：`5.87%`
  - `ClassIntArrayReader.fromString(...)`：`5.57%`
  - `IntArrayReader.fromString(...)`：`2.66%`
  - `NewArrayReader.fromString(...)`：`2.26%`
- JFR allocation-by-site 前列：
  - `StringUTF16.compress(...)`：`39.93%`
  - `HashMap.resize()`：`9.44%`
  - `Stream.getNumberString()`：`7.12%`
  - `ArrayList.grow(int)`：`6.72%`
  - `Array.newInstance(Class, int)`：`6.38%`
  - `BeanReader_0.fromString(...)`：`6.00%`
  - `Stream.getString()`：`5.32%`
  - `ClassIntArrayReader.fromString(...)`：`3.08%`

判断：

- `B/op` 从第 1 步的 `5537.074` 到本步 `5536.744`，基本不变，没有引入额外分配压力。
- JFR 吞吐从第 1 步的 `441.043 ± 2.533 ops/ms` 到本步 `442.761 ± 6.586 ops/ms`，差异很小，不能认为有明确性能收益。
- `BenchmarkBig` 中仍有 `int[][]`、`Integer[][]`、`ArrayList<String>[]`，这些不在本步“一维数组”覆盖范围内，所以 `ClassIntArrayReader`、`IntArrayReader`、`NewArrayReader` 仍然出现在热点中。
- 一维数组内联后，对应成本会进入 `BeanReader_*` 生成代码本身，因此 `BeanReader_0.fromString` 占比上升不能直接解释为退化；但结合吞吐看，本步收益不明显。

状态：已完成，收益不明确。

### 第 3 步：字段专属 Collection reader

目标：

- 对明确字段类型的 `ArrayList<T>` / `List<T>` 生成字段专属循环。
- 普通元素走内联基础类型或缓存的元素 reader。

判断标准：

- `CollectionReader.fromString` 在 JFR 中下降。
- compile 模式吞吐改善或至少不退化。

结果：

- `mvn test` 通过：`Tests run: 133, Failures: 0, Errors: 0, Skipped: 1`
- JMH GC：`BenchmarkBig.testCompile = 435.524 ± 21.180 ops/ms`
- JMH GC：`BenchmarkBig.testCompile:·gc.alloc.rate.norm = 5536.728 ± 0.199 B/op`
- JMH GC：`BenchmarkBig.testNew = 536.568 ± 12.131 ops/ms`
- JMH GC：`BenchmarkBig.testNew:·gc.alloc.rate.norm = 5664.521 ± 0.122 B/op`
- JFR：`BenchmarkBig.testCompile = 437.705 ± 12.012 ops/ms`
- JFR hot methods 前列：
  - `Stream.getString()`：`17.81%`
  - `BeanReader_0.fromString(...)`：`12.18%`
  - `Stream.ignoreSymbol()`：`11.97%`
  - `String.charAt(int)`：`11.26%`
  - `Stream.getNameIndex(FieldIndexNode)`：`7.97%`
  - `BeanReader_9.fromString(...)`：`7.76%`
  - `ClassIntArrayReader.fromString(...)`：`5.83%`
  - `Stream.readUnKnowType()`：`4.82%`
  - `CollectionReader.fromString(...)`：`3.25%`
  - `NewArrayReader.fromString(...)`：`2.49%`
  - `IntArrayReader.fromString(...)`：`2.38%`
- JFR allocation-by-site 前列：
  - `StringUTF16.compress(...)`：`35.52%`
  - `Array.newInstance(Class, int)`：`8.62%`
  - `BeanReader_0.fromString(...)`：`8.34%`
  - `HashMap.newNode(...)`：`8.20%`
  - `Stream.getString()`：`7.20%`
  - `Double.valueOf(String)`：`5.06%`
  - `ArrayList.grow(int)`：`4.87%`
  - `HashMap.resize()`：`4.72%`
  - `Stream.getNumberString()`：`4.26%`
  - `Stream.readUnKnowType()`：`4.00%`

判断：

- `CollectionReader.fromString` 从第 2 步的 `5.87%` 降到本步 `3.25%`，字段专属集合读取确实覆盖了 `ArrayList<String>`、`ArrayList<NestData>`、空 `ArrayList<String>` 这类字段。
- 剩余 `CollectionReader` 主要来自未覆盖路径，例如 `ArrayList<String>[] lists` 这类数组内的集合元素，仍由 `NewArrayReader` 的元素 reader 间接调用。
- `B/op` 仍稳定在 `5536.7` 左右，没有额外分配。
- JFR 吞吐从第 2 步 `442.761 ± 6.586 ops/ms` 到本步 `437.705 ± 12.012 ops/ms`，没有明确提升；热点下降是真实的，但收益被 `Stream.getString`、字段名解析、数组/未知类型和 map 分配等成本覆盖。

状态：已完成，热点改善明确，吞吐收益不明确。

### 第 4 步：字段专属 Map reader

目标：

- 对 `Map<String, T>` / `HashMap<String, T>` 生成字段专属对象读取循环。
- 先覆盖 String key，value 为基础类型、String、普通 POJO 的情况。

判断标准：

- `MapReader.fromString`、`HashMap.resize/newNode` 相关热点变化明确。

结果：

- `mvn test` 通过：`Tests run: 133, Failures: 0, Errors: 0, Skipped: 1`
- JMH GC：`BenchmarkBig.testCompile = 441.971 ± 17.231 ops/ms`
- JMH GC：`BenchmarkBig.testCompile:·gc.alloc.rate.norm = 5536.728 ± 0.121 B/op`
- JMH GC：`BenchmarkBig.testNew = 523.814 ± 6.750 ops/ms`
- JMH GC：`BenchmarkBig.testNew:·gc.alloc.rate.norm = 5664.625 ± 0.159 B/op`
- JFR：`BenchmarkBig.testCompile = 445.499 ± 2.353 ops/ms`
- JFR hot methods 前列：
  - `BeanReader_0.fromString(...)`：`16.74%`
  - `Stream.getString()`：`14.95%`
  - `String.charAt(int)`：`13.55%`
  - `Stream.ignoreSymbol()`：`8.52%`
  - `BeanReader_8.fromString(...)`：`8.42%`
  - `Stream.getNameIndex(FieldIndexNode)`：`7.92%`
  - `ClassIntArrayReader.fromString(...)`：`5.08%`
  - `Stream.readUnKnowType()`：`4.58%`
  - `CollectionReader.fromString(...)`：`3.64%`
  - `IntArrayReader.fromString(...)`：`2.74%`
  - `NewArrayReader.fromString(...)`：`2.19%`
- JFR allocation-by-site 前列：
  - `StringUTF16.compress(...)`：`33.40%`
  - `HashMap.newNode(...)`：`8.43%`
  - `BeanReader_0.fromString(...)`：`6.98%`
  - `ArrayList.grow(int)`：`6.90%`
  - `Array.newInstance(Class, int)`：`6.33%`
  - `HashMap.resize()`：`6.14%`
  - `Stream.getString()`：`6.01%`
  - `Stream.getNumberString()`：`5.68%`
  - `ClassIntArrayReader.fromString(...)`：`5.30%`
  - `FloatingDecimal.readJavaFormatString(...)`：`4.90%`

判断：

- `MapReader.fromString` 已不在 JFR hot methods 前列，字段 `HashMap<String, String> map` 已由生成代码直接读取。
- `HashMap.newNode`、`HashMap.resize` 仍然存在，因为最终结果对象仍然需要实际 map entry 分配；另外 `Object data` 的未知类型解析也会继续制造 `HashMap`/`ArrayList` 分配。
- `B/op` 仍稳定在 `5536.7` 左右，没有额外分配。
- JFR 吞吐从第 3 步 `437.705 ± 12.012 ops/ms` 到本步 `445.499 ± 2.353 ops/ms`，这一轮数据更稳且略好；但只覆盖了一个 map 字段，不能把全部提升都归因给该改动。

状态：已完成，热点改善明确，吞吐略好但仍需谨慎看待。

### 第 5 步：Stream 基础解析优化

目标：

- 整数族读取不再通过 `getNumberString()` 创建临时 `String`，直接从 `char[]` 解析 `int`、`long`、`short`、`byte` 及对应 boxed 类型。
- `ignoreSymbol()` 增加非空白字符快路径，紧凑 JSON 下直接返回当前字符。
- `float`、`double` 暂时保持原实现，避免小数、指数和边界语义一次性扩大。

判断标准：

- `BenchmarkBig.testCompile` 的 `B/op` 明确下降。
- JFR 中 `String.charAt(int)`、`Stream.getNumberString()`、`StringUTF16.compress(...)` 相关占比下降。
- `mvn test` 通过。

结果：

- `mvn test` 通过：`Tests run: 133, Failures: 0, Errors: 0, Skipped: 1`
- JMH GC：`BenchmarkBig.testCompile = 623.932 ± 22.425 ops/ms`
- JMH GC：`BenchmarkBig.testCompile:·gc.alloc.rate.norm = 4504.594 ± 0.137 B/op`
- JMH GC：`BenchmarkBig.testNew = 729.229 ± 28.356 ops/ms`
- JMH GC：`BenchmarkBig.testNew:·gc.alloc.rate.norm = 4488.408 ± 0.069 B/op`
- JFR：`BenchmarkBig.testCompile = 652.664 ± 4.749 ops/ms`
- JFR hot methods 前列：
  - `Stream.getIntegralLong()`：`17.78%`
  - `BeanReader_0.fromString(...)`：`17.15%`
  - `Stream.getString()`：`12.16%`
  - `Stream.getNameIndex(FieldIndexNode)`：`12.06%`
  - `BeanReader_8.fromString(...)`：`5.35%`
  - `Stream.readUnKnowType()`：`4.99%`
  - `Stream.ignoreSymbol()`：`4.63%`
  - `CollectionReader.fromString(...)`：`3.48%`
  - `FloatingDecimal.readJavaFormatString(...)`：`3.17%`
  - `NewArrayReader.fromString(...)`：`3.12%`
  - `Stream.getNumberString()`：`0.83%`
  - `String.charAt(int)`：`0.42%`
- JFR allocation-by-site 前列：
  - `StringUTF16.compress(...)`：`21.92%`
  - `Array.newInstance(Class, int)`：`11.61%`
  - `Stream.getString()`：`11.58%`
  - `BeanReader_0.fromString(...)`：`6.55%`
  - `String.<init>(char[], int, int, Void)`：`5.56%`
  - `ClassIntArrayReader.fromString(...)`：`5.34%`
  - `Stream.getNumberString()`：`5.26%`

判断：

- 这是目前最明确的端到端收益。相对第 4 步，JMH GC 吞吐从 `441.971` 提升到 `623.932 ops/ms`，JFR 吞吐从 `445.499` 提升到 `652.664 ops/ms`。
- `B/op` 从第 4 步的 `5536.728` 降到 `4504.594`，说明整数临时字符串确实是主要分配来源之一。
- `String.charAt(int)` 从第 4 步 `13.55%` 降到 `0.42%`，`Stream.getNumberString()` 从热点前列降到 `0.83%`，方向命中。
- `Stream.getIntegralLong()` 成为新热点，说明成本从字符串创建和 JDK parse 迁移到了直接解析循环本身。
- `float`、`double` 仍走 `getNumberString()`，所以 `FloatingDecimal` 和部分 `StringUTF16.compress` 仍存在。

状态：已完成，收益明确。

### 第 6 步：多维数组和复杂泛型评估

目标：

- 基于前几步结果判断是否继续覆盖多维数组、嵌套泛型集合、复杂 wildcard。
- 只在收益明确时继续扩展代码生成复杂度。

状态：未开始。
